{
  "name": "research-kit",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Renders research pieces. Holds no brand values: every colour, size and weight is named through the token contract and supplied by a consuming site's adapter.",
  "exports": {
    "./package.json": "./package.json",
    "./components/*.astro": "./components/*.astro",
    "./lib/exhibits": "./lib/exhibits.ts",
    "./contract/*.json": "./contract/*.json",
    "./contract/*.mjs": "./contract/*.mjs"
  },
  "peerDependencies": {
    "astro": "^5.0.0"
  }
}
