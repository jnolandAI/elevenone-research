{
  "name": "research-kit",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Renders research pieces. Holds no brand colour: every colour is named through the token contract and supplied by a consuming site's adapter. See README.md for what else a consuming site must supply.",
  "exports": {
    "./package.json": "./package.json",
    "./components/*.astro": "./components/*.astro",
    "./lib/exhibits": "./lib/exhibits.ts",
    "./lib/us-states": "./lib/us-states.ts",
    "./contract/*.json": "./contract/*.json",
    "./contract/*.mjs": "./contract/*.mjs"
  },
  "peerDependencies": {
    "astro": "^5.0.0"
  }
}
